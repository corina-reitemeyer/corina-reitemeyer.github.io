export default interface ContactData {
  email: string
  calendlyUrl: string
  eyebrow: string
  title: string
  lede: string
  marginNote: string
  emailHint: string
  meetingLabel: string
  meetingHint: string
}
